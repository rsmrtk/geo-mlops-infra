image:
  repository: ghcr.io/mlflow/mlflow
  tag: v2.16.0
  pullPolicy: IfNotPresent

service:
  port: 5000

persistence:
  size: 5Gi
  storageClass: ""  # use cluster default

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
